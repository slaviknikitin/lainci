let level = false;

// Conditional statement based on the value of 'level'
if (level) {
    console.log('Level is true');
} else {
    console.log('Level is false'); // This line will be executed
}

// Using 'level' as a flag for a specific behavior
if (!level) {
    // Perform some action when 'level' is false
    console.log('Special behavior when level is false');
}
